name: Terraform Security Audit

on:
  # Trigger on pull requests that change .tf files
  pull_request:
    paths:
      - '**.tf'
  # Trigger on pushes to the main branch that change .tf files
  push:
    branches:
      - main
    paths:
      - '**.tf'
  # Allow the workflow to be run manually from the Actions tab
  workflow_dispatch:
    inputs:
      filepath:
        description: 'Path to the Terraform file to audit'
        required: true
        default: 'test'

permissions:
  pull-requests: write

jobs:
  iac-security-audit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run AI Security Auditor
        id: audit_step
        uses: ./ 
        with:
          filepath: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.filepath || 'test' }}
        env:
          HUGGING_FACE_HUB_TOKEN: ${{ secrets.HUGGING_FACE_HUB_TOKEN }}

      - name: Check Audit Results
        if: ${{ failure() && steps.audit_step.outcome == 'failure' }}
        run: |
          echo "❌ AI Security Auditor found vulnerabilities. Failing the workflow."
  
  add-pr-comment:
    needs: iac-security-audit
    runs-on: ubuntu-latest
    # This condition ensures the job only runs on pull request events
    if: github.event_name == 'pull_request'
    steps:
      - name: Post Audit Results as a PR Comment
        uses: actions/github-script@v7
        with:
          script: |
            const auditResults = '${{ steps.audit_step.outputs.audit_results }}';
            const commentBody = `AI Security Audit Results:\n\n${auditResults}`;
            github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: commentBody
            });